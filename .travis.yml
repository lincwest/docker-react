sudo: required

services:
    - docker

before_install:
    - echo "BEFORE INSTALL START"
    - gem install bundler
    - echo "BEFORE INSTALL END"
    - docker build -t lincwest/docker-react -f Dockerfile.dev .

script:
    - docker run lincwest/docker-react npm run test -- --coverage