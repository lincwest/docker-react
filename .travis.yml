sudo: required
services:
    - docker

before_install:
    - docker build -t lincx/docker-react -f Dockerfile.dev .

script:
    - docker run lincx/docker-react npm run test -- --coverage

language: generic